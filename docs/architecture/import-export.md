# Import & Export Engine Architecture — IAP

`ImportEngine` dan `BatchOperationService` mengelola impor masal CSV dengan validasi dan pratinjau (*preview*), serta eksekusi operasi masal terdorong antrean (*queue-driven bulk operations*).
