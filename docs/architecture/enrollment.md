# Enrollment Engine Architecture — IAP

`EnrollmentEngine` mengelola pendaftaran peserta ke dalam kursus akademik dengan status enum `EnrollmentStatus` (`active`, `completed`, `cancelled`, `suspended`).
Metode pendukung: `enrollStudent`, `bulkEnroll`, dan `cancelEnrollment`.
